#include <stdio.h>
#include <stdlib.h>
#include <math.h>

#define RESTO(x,y) (x%y)
#define ABS(x) (x >= 0 ? x:-x)
#define TRUNCA(x) ((int) (x/1))

int main (int argc, char* argv[]) {
if () {
RESTOif () {
} else {
}
}
return 0;
}
